{{#each books as |book|}}
  {{log book}}

  <div>
    <h1 data-id="{{book.id}}"> <small>Title: {{book.title}}</small></h1>
    {{#if book.author}}
      {{book.author}}
    {{else}}
      No author provided.
    {{/if}}
  </div>

{{/each}}
